using UnityEngine;
using Specific.Location;

public class XC_Script : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*Marker marker = SavePointManager.GetMarker();
        Debug.Log(marker.marks[0].key);*/
    }
}
